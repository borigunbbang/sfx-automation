# PRD: 방송 예능 자막 효과음 자동 삽입 프로그램 (가칭: AutoSFX)

## 1. 배경 및 문제 정의
방송 예능 편집에서는 자막에 애니메이션 효과(슬라이드 등장, 3D 등장 등)를 적용할 때, 그 효과 종류에 맞는 정해진 패턴의 효과음(휙 지나가는 소리, 팝 소리 등)을 함께 삽입하는 경우가 많다.

- 자막 효과 종류(슬라이드, 3D 등장, 통통 튐 등)마다 어울리는 효과음 종류가 대체로 정해져 있음
- 편집자는 이 매칭 작업을 영상 전체에 걸쳐 반복적으로, 수작업으로 수행함
- 반복적이지만 규칙성이 뚜렷한 작업이라 자동화 가능성이 큼

이 프로그램은 이 반복 작업을 자동화하는 것을 목표로 한다.

## 2. 목표 (Goals)
- 영상 내 자막 효과가 등장하는 시점을 자동으로 감지한다.
- 감지된 자막 효과의 유형(슬라이드/3D 등)을 자동으로 분류한다.
- 분류된 유형에 맞는 효과음을 기존 보유 효과음 라이브러리에서 자동으로 선택/매칭한다.
- 사용자가 결과를 빠르게 확인하고 필요한 부분만 보정한 뒤, 최종 결과물을 받을 수 있게 한다.
- 반복 작업에 드는 시간을 획기적으로 단축한다.

## 3. MVP 범위에서 제외하는 것 (Non-goals)
- 모든 자막 효과 타입을 완벽히 커버하는 것 (초기에는 소수 타입만 지원)
- 특정 편집 프로그램에 완전 자동 연동되는 실시간 플러그인 (추후 단계로 미룸)
- 자막 "텍스트 내용"의 의미를 분석해 효과음을 추천하는 것 (이번 범위는 오직 "효과 타입" 기준 매칭)
- 배경음악(BGM) 자동 편집
- 효과음이 섞인 완성본 오디오/영상 파일을 자동 생성하는 것 (한번 섞이면 편집자가 나중에 개별 효과음을 조정하기 어려워지므로, MVP는 편집 통제권을 편집자에게 남겨두는 "마커+파일명 표" 방식을 택함. 완성본 자동 생성은 향후 확장 참고)
- **구독/결제 시스템** (가입된 사용자면 누구나 제한 없이 사용 — 결제 연동은 이번 MVP 범위 밖)

## 3.5 서비스 형태 (결정 사항)
- 로컬 프로그램이 아니라 **웹 페이지로 배포하는 서비스**로 운영한다.
- **회원가입한 사용자만** 로그인해서 사용할 수 있다 (비로그인 사용자는 사용 불가).
- 회원가입/로그인은 직접 구현하지 않고 **Supabase Auth**(검증된 인증 서비스)에 위임한다.

## 4. 타겟 사용자
방송 예능 프로그램을 편집하며, 자막 효과에 맞춰 효과음을 반복적으로 삽입하는 편집자 본인(및 팀).

## 5. 핵심 사용자 시나리오
0. (사전 준비, 최초 1회) 편집자가 자주 쓰는 자막 스타일별로 "다 나타난 직후(정지된) 상태"의 스크린샷을 캡처해서, 어울리는 효과음 폴더와 짝지어 등록해둔다.
1. 편집자가 자막이 이미 합성(burn-in)된 영상 파일(mp4)을 업로드한다.
2. 시스템이 영상을 분석해 "자막 효과 등장 시점" 목록을 자동으로 추출한다.
3. 각 시점의 "다 나타난 직후" 대표 장면을, 등록해둔 예시 사진들과 비교해 가장 닮은 타입으로 분류한다.
4. 분류된 타입에 맞는 효과음 파일을 사용자의 효과음 라이브러리 폴더에서 자동으로 선택한다.
5. 사용자가 화면에서 결과를 미리 듣고, 필요하면 개별 항목을 수정한다 (타입 재지정, 다른 효과음으로 교체, 이벤트 삭제/추가, 닮은 예시가 없어 미분류된 경우 새 예시 사진으로 등록).
6. 최종 결과물을 받는다: 타임코드 + 효과 타입 + 매칭된 효과음 파일명 목록 (EDL/CSV — REAPER 등 외부 프로그램의 마커 가져오기 기능과 호환)

## 6. 핵심 기능 (MVP 범위)
| # | 기능 | 설명 |
|---|------|------|
| 6.0 | 참고 이미지 등록 | 사용자가 미리 캡처한 자막 스타일별 예시 사진을 효과음 폴더와 짝지어 등록 |
| 6.1 | 영상 업로드 & 전처리 | mp4 업로드, 분석용 프레임 추출 |
| 6.2 | 자막 등장 이벤트 자동 감지 | 컴퓨터 비전으로 자막 효과 시작/종료 시점 추출 |
| 6.3 | 효과 타입 분류 | 이벤트의 "다 나타난 직후" 대표 장면을 등록된 예시 사진들과 비교해 가장 닮은 타입으로 분류 |
| 6.4 | 효과음 자동 매칭 | 분류된 타입의 폴더에서 효과음 선택 |
| 6.5 | 결과 미리보기 & 수동 보정 UI | 영상 재생 + 마커 + 오디오 미리듣기 + 수정 |
| 6.6 | 결과물 내보내기 | 타임코드+효과 타입+파일명 목록 (EDL/CSV) |

## 7. 가정 및 제약사항
- 입력 영상은 자막이 이미 렌더링(burn-in)되어 있는 mp4 파일이다 (별도 자막 트랙 없음).
- 효과음 라이브러리는 폴더별로 타입이 구분되어 있다 (예: `/sfx/slide`, `/sfx/3d`).
- 효과 타입 분류는 "규칙(움직임 분석)"이 아니라 **사용자가 미리 등록한 예시 사진과의 이미지 비교(매칭)** 방식으로 이루어진다.
- 예시 사진은 자막이 "다 나타난 직후, 움직임이 멈춘 정지 상태"를 캡처한 것으로 통일한다.
- 예시 사진 등록은 자동이 아니라 사용자가 초기에 직접 캡처해서 넣어두는 것으로 시작한다 (등록되지 않은 새 스타일은 자동 인식되지 않음).
- 지원할 효과 타입은 **1개(테스트용)** 로 시작해 파이프라인이 정상 동작하는지부터 검증하고, 이후 개수를 늘린다.
- 컴퓨터 비전 기반 감지/분류는 100% 정확하지 않으므로, 사용자 보정 UI가 반드시 필요하다.
- 특정 편집 프로그램(NLE)로의 직접 자동 삽입은 MVP 이후 단계이며, 1차는 범용 EDL/CSV 출력으로 대체한다.
- 업로드된 영상/효과음 라이브러리/참고 이미지는 **사용자별로 분리**되어 저장된다 (다른 사용자의 자료를 볼 수 없음).

## 8. 성공 지표 (MVP 검증 기준)
- 자막 등장 이벤트 감지 재현율(recall) ≥ 90% (테스트 영상 기준)
- 효과 타입 분류 정확도 ≥ 80% (지원 타입 한정)
- 자동 매칭 결과를 보정 없이 그대로 채택하는 비율
- 기존 수동 작업 대비 체감 소요 시간 단축

## 9. Open Questions (다음 단계에서 확정 필요)
- [x] 처음 등록할 예시 사진(자막 스타일) 몇 개로 시작할지 → **1개로 시작 (테스트 목적)**
- [x] 분류(이미지 매칭) 로직 튜닝을 위한 샘플 영상 확보 가능 여부 → **보유 중**
- [ ] 실제 효과음 라이브러리의 폴더 구조/네이밍 규칙 공유
- [ ] 최종 결과물을 REAPER로 보낼지, 사이트 자체 편집기로 완결할지 방향 확정

## 10. 향후 확장 (MVP 이후)
- REAPER ReaScript 연동으로 원클릭 타임라인 삽입
- 지원 효과 타입 확대 및 정확도 개선 (필요시 머신러닝 기반 분류기 도입)
- 자막 텍스트 의미 기반 효과음 추천
- 다중 NLE 지원
- (선택적 기능으로) 효과음이 섞인 완성본 오디오/영상을 바로 뽑아주는 옵션 추가 — 단, 기본 결과물은 계속 "마커+파일명 표"로 유지
