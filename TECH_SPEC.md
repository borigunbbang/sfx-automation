# 기술 명세서: AutoSFX MVP

> 이 문서는 [PRD.md](./PRD.md)에서 정의한 MVP 범위를 어떻게 구현할지 다룬다.

## 1. 시스템 개요 & 아키텍처
파이프라인 방식으로 구성한다.

```
(사전 준비, 최초 1회)
[참고 이미지 등록: 자막 스타일별 "정지 상태" 캡처 + 효과음 폴더 연결]

(영상 처리 파이프라인)
[영상 업로드]
    ↓
[전처리: 프레임 추출 (ffmpeg)]
    ↓
[자막 이벤트 감지 (OpenCV, ROI 프레임 차분) → 시작/종료 + 대표(정지) 프레임 추출]
    ↓
[효과 타입 분류 (대표 프레임을 등록된 참고 이미지와 비교/매칭)]
    ↓
[SFX 매칭 엔진 (폴더 기반 라이브러리 매핑)]
    ↓
[웹 프리뷰 & 보정 UI]
    ↓
[출력 생성: EDL/CSV (타임코드 + 효과 타입 + 파일명)]
```

> MVP는 효과음을 실제로 섞은 완성본 파일을 만들지 않는다. 편집자가 나중에 개별 효과음을 조정할 수 있도록, "어디에 어떤 효과음을 넣어야 하는지"를 알려주는 표(CSV/EDL)까지만 만든다. 완성본 자동 생성은 향후 확장(섹션 9) 참고.

## 2. 기술 스택 (확정 — 웹 서비스 기준)

> **범위 변경**: 처음엔 로컬 실행 개인용 도구로 시작했지만, **회원가입한 사용자만 쓸 수 있는 웹 서비스**로 방향이 바뀌었다 (결제/구독은 이번 MVP 범위 밖). 아래는 이 방향에 맞춰 확정한 스택이다.

| 구성 요소 | 선택 | 이유 |
|---|---|---|
| 백엔드 | **FastAPI (Python)** | 지금까지 만든 `src/` 코드(이벤트 감지·매칭 로직)를 거의 그대로 재사용 |
| 회원가입/로그인 | **Supabase Auth** | 비밀번호 저장·이메일 인증 등 보안이 걸린 부분을 직접 구현하지 않고 검증된 서비스에 위임 |
| DB | **Supabase Postgres** | Auth와 같은 서비스 안에서 바로 연동, 무료 티어로 시작 가능 |
| 파일 저장소 | **Supabase Storage** | 업로드 영상, 효과음 라이브러리, 참고 이미지 저장 |
| 프론트엔드 | **React + Vite (SPA)** | 로그인/업로드/결과 확인 화면 정도의 단순한 구성이라 Next.js 같은 풀프레임워크는 과함 |
| 영상/오디오 처리 | `ffmpeg`, `imageio`, `imagehash` (기존 프로토타입과 동일) | M0~M3에서 이미 검증됨 |
| 백그라운드 처리 | 초기: DB에 "처리 상태" 컬럼을 두고 워커가 주기적으로 폴링 → 필요해지면 Redis+큐(예: `arq`)로 교체 | 초반 트래픽이 적을 것으로 예상되어 가장 단순한 방식으로 시작 |
| 배포 | 프론트=**Vercel**, 백엔드=**Render.com 또는 Fly.io** | 둘 다 무료/저비용 티어 제공, Python 백그라운드 워커 지원 |

- **자막 영역(ROI) 검출**: MVP는 사용자가 자막 위치를 수동 지정(사각형 드래그) → 추후 자동 텍스트 검출(OCR/EAST 등)로 개선

## 3. 웹 서비스 아키텍처

```
[사용자 브라우저 (React SPA)]
    ↕ (로그인/가입은 Supabase Auth와 직접 통신)
    ↕ HTTPS API
[FastAPI 백엔드]
    ├─ 요청 시 Supabase에서 발급한 JWT를 검증 (로그인한 사용자인지 확인)
    ├─ 영상 업로드 → Supabase Storage에 저장 + DB에 "처리 대기중" 상태로 기록
    ├─ 4장의 분석 파이프라인 실행 (기존 M1~M3 로직 그대로)
    └─ 결과(이벤트/매칭 결과)를 Supabase Postgres에 저장

[Supabase]
    ├─ Auth: 회원가입/로그인/세션 관리
    ├─ Postgres: 사용자, 프로젝트, 이벤트, 참고 이미지 메타데이터
    └─ Storage: 업로드 영상, 효과음 파일, 참고 이미지 파일
```

### 3.1 인증 흐름
1. 사용자가 프론트엔드에서 Supabase Auth로 직접 회원가입/로그인 (이메일+비밀번호로 시작, 소셜 로그인은 향후 확장)
2. 로그인 성공 시 Supabase가 발급한 JWT를 프론트엔드가 보관
3. 이후 모든 API 요청에 이 JWT를 담아 FastAPI로 전송
4. FastAPI는 Supabase의 공개키로 JWT를 검증해서 "누가 요청했는지" 확인 (비밀번호 자체는 FastAPI가 다루지 않음)
5. 가입하지 않은 사용자는 로그인 화면 이상으로 진입 불가 (PRD 결정 사항 — 결제/구독 체크는 없음, 로그인 여부만 확인)

### 3.2 데이터 소유 구조
모든 데이터(업로드 영상, 참고 이미지, 이벤트 결과)는 `user_id`로 소유자가 구분된다 — 즉 A 사용자가 등록한 참고 이미지/효과음 라이브러리를 B 사용자는 볼 수 없다 (사용자별로 완전히 분리된 프로젝트/라이브러리).

### 3.3 백그라운드 처리 (영상 분석)
영상 분석은 1~3분 정도 걸리므로, 업로드 요청에 대해 바로 결과를 주지 않고:
1. 업로드 시 DB에 `status: pending`인 작업 레코드 생성, 즉시 응답 반환
2. 워커(별도 프로세스, 처음엔 단순 폴링 루프)가 `pending` 작업을 찾아 처리 시작 (`status: processing`)
3. 완료되면 `status: done` + 결과 저장, 프론트엔드는 주기적으로 상태를 조회(polling)해서 완료되면 결과 화면으로 전환
4. 트래픽이 늘어나면 Redis 기반 큐(`arq`, `celery` 등)로 교체 (향후 확장)

## 4. 모듈별 설계 (분석 엔진 — M1~M3 프로토타입에서 검증됨)

### 4.1 영상 업로드 & 전처리
- 입력: mp4 파일 + 사용자가 지정한 자막 ROI 좌표
- `ffmpeg`으로 저FPS(예: 10fps)로 ROI 영역만 크롭한 프레임 시퀀스를 추출해 연산량을 줄인다
- 산출물: 프레임 이미지 시퀀스 + 프레임 번호→타임스탬프 매핑

### 4.2 자막 이벤트 감지
- 방법: 인접 프레임 간 ROI 픽셀 차이(절대차 또는 SSIM)를 계산하고, 임계값 이상의 변화가 일정 시간 이상 지속되는 구간을 "자막 이벤트"로 판정
- 이벤트의 시작/종료 시각(애니메이션 재생 구간)을 추출
- **대표(정지) 프레임 추출**: 이벤트 종료 시각 직후, 인접 프레임 간 차이가 다시 임계값 이하로 떨어져 "움직임이 멈춘" 첫 프레임을 대표 프레임으로 저장 (=3.3 분류 단계에서 비교에 사용할 이미지)
- 노이즈 제거: 최소 지속시간 필터, 짧은 간격의 이벤트는 하나로 병합(디바운스)

### 4.3 효과 타입 분류 (참고 이미지 매칭 방식)
- **사전 등록 (사용자, 최초 1회)**: 자막 스타일별로 "다 나타난 직후 정지 상태"를 캡처한 참고 이미지를 등록. 각 참고 이미지는 효과 타입 라벨 + 효과음 폴더와 짝지어짐
  ```yaml
  references:
    - image: ./references/slide_sample.png
      effect_type: slide
      sfx_folder: ./sfx_library/slide
    - image: ./references/3d_sample.png
      effect_type: 3d
      sfx_folder: ./sfx_library/3d
  ```
- **분류 로직**: 4.2에서 추출한 이벤트별 대표(정지) 프레임을, 등록된 모든 참고 이미지와 이미지 유사도 비교
  - 후보 알고리즘: OpenCV 템플릿 매칭(`matchTemplate`) 또는 특징점 매칭(ORB), 필요시 perceptual hash(`imagehash`) 등 — 자막은 실사 사진이 아니라 그래픽 요소라 유사도 비교가 비교적 안정적일 것으로 예상, 실제 샘플로 검증 필요
  - 가장 유사도가 높은 참고 이미지의 `effect_type`을 채택 (유사도가 임계값 미만이면 "미분류"로 남김)
- **미분류 처리**: 어떤 참고 이미지와도 충분히 닮지 않은 이벤트는 `effect_type: unknown`으로 표시 → 4.5 보정 UI에서 사용자가 직접 타입을 지정하고, 원하면 그 대표 프레임을 새 참고 이미지로 등록 (다음부터 자동 인식됨)
- 출력 형식: 이벤트별 `{start, end, representative_frame, effect_type, match_score}`

### 4.4 SFX 매칭 엔진
- 4.3에서 이벤트가 매칭된 참고 이미지의 `sfx_folder`를 그대로 사용 (별도 매핑 설정 불필요 — 참고 이미지 등록 시 이미 폴더가 지정되어 있음)
- 매칭 로직: 해당 폴더에서 파일을 선택
  - 1차: 순환(round-robin) 또는 최근 사용 파일을 회피하는 랜덤 선택 (반복 청취감 감소)
- 향후 확장: 볼륨/타이밍 미세 오프셋 설정, 사용 빈도 가중치 등

### 4.5 프리뷰 & 보정 UI
- 영상 플레이어 + 하단 타임라인에 감지된 이벤트를 마커로 표시
- 마커 클릭 시 가능한 동작:
  - 매칭된 효과음 미리듣기
  - 효과 타입 변경(드롭다운)
  - 다른 효과음 파일로 교체
  - 이벤트 삭제 / 수동 추가
  - (미분류 이벤트의 경우) 타입을 지정하면서, 해당 대표 프레임을 새 참고 이미지로 등록 → 이후 같은 스타일은 자동 인식
- 영상 재생과 SFX 프리뷰를 Web Audio API로 동기 재생

### 4.6 출력 생성
- **EDL/CSV 내보내기** (MVP의 유일한 출력): `timestamp, effect_type, sfx_filename` 형태의 표 파일
  - REAPER의 마커/리전 임포트 CSV 포맷과 호환되도록 형식을 맞춰, 추후 REAPER 연동 시에도 재사용 가능하게 설계
  - 실제 오디오 파일을 섞지 않으므로, 편집자는 이 표를 보고 원하는 편집 프로그램에서 직접(또는 M9 자동 연동으로) 효과음을 배치한다
  - (믹스된 완성본 오디오/영상 생성은 MVP 범위에서 제외 — 섹션 9 향후 확장 참고)

## 5. 데이터 모델
```
User {
  id: string          // Supabase Auth의 사용자 id 그대로 사용
  email: string
}

ReferenceImage {
  id: string
  user_id: string           // 소유자 (3.2 데이터 소유 구조)
  image_path: string        // Supabase Storage 경로, 자막 "정지 상태" 캡처 이미지
  effect_type: string
  sfx_folder: string        // Supabase Storage 경로
}

Event {
  id: string
  project_id: string
  start_ms: int
  end_ms: int
  representative_frame_path: string  // 4.2에서 추출한 정지 프레임
  effect_type: string        // 매칭된 참고 이미지의 타입, 없으면 "unknown"
  match_score: float
  matched_sfx_path: string
}

Project {
  id: string
  user_id: string           // 소유자
  video_path: string        // Supabase Storage 경로
  roi: {x, y, width, height}
  status: "pending" | "processing" | "done" | "failed"  // 3.3 백그라운드 처리 상태
  reference_images: ReferenceImage[]
  events: Event[]
}
```

## 6. 개발 단계 (Milestones) — 차근차근 진행

**로컬 프로토타입 (완료)**

| 단계 | 목표 | 완료 기준 | 상태 |
|------|------|-----------|------|
| M0 | 프로젝트 스캐폴딩, 영상 읽기 환경 확인 | 샘플 영상에서 프레임 추출 성공 | ✅ 완료 |
| M1 | ROI 기반 자막 "이벤트 발생 시점" + 대표(정지) 프레임 감지 | 테스트 영상에서 이벤트 타임스탬프 목록 출력 | ✅ 완료 |
| M2 | 참고 이미지 등록 + 이미지 매칭 기반 효과 타입 분류 (1개 타입) | 이벤트별 타입 라벨 부여 (정확도는 낮음, 튜닝 필요) | ✅ 완료 (프로토타입 수준) |
| M3 | SFX 매칭 엔진 + CSV/EDL 출력 | 타임스탬프+파일명 CSV 생성 | ✅ 완료 |

**웹 서비스 전환 (다음 단계)**

| 단계 | 목표 | 완료 기준 |
|------|------|-----------|
| M4 | Supabase 프로젝트 생성 + Auth 연동 (회원가입/로그인 화면) + 보안 기본 설정(**섹션 10 체크리스트 1~5번**) | 가입/로그인한 사용자만 다음 화면 진입 가능, RLS·비밀키·이메일확인 설정 완료 |
| M5 | FastAPI에 M1~M3 파이프라인을 API로 감싸기 (업로드 API, 백그라운드 처리, 상태/결과 조회 API) | 브라우저 없이 API 호출만으로 업로드→분석→결과 조회 가능 |
| M6 | 프론트엔드: 업로드 화면 + 결과 확인/보정 UI (영상+마커+오디오 미리듣기) | 로그인한 사용자가 웹에서 영상 업로드~결과 확인까지 가능 |
| M7 | 보정 기능(타입 변경/교체/삭제/추가) + 최종 CSV/EDL 재생성 | 보정 반영된 최종 표 다운로드 가능 |
| M8 (실제 오픈 직전 필수) | **섹션 10 체크리스트 6~8번 최종 점검** (도메인/HTTPS, 백업·모니터링, 무료 티어 한계 확인) | 실사용자에게 공개해도 안전한 상태 확인 |
| M9 (Stretch) | REAPER ReaScript 연동 | 원클릭으로 REAPER 타임라인에 삽입 |
| M10 (Stretch, 선택) | 믹스된 완성본 오디오/영상 출력 옵션 | ffmpeg로 효과음이 섞인 mp4/wav 생성 |

각 단계는 이전 단계가 동작하는 상태에서 다음 단계로 넘어간다.

## 7. 리스크 & 오픈 이슈
- 이미지 매칭 정확도 한계: 같은 스타일이라도 자막 텍스트 내용/길이가 다르면 유사도가 낮게 나올 수 있음 → 실제 샘플로 임계값/알고리즘 튜닝 필요 (프로토타입 테스트에서 확인된 이슈, 현재는 "정확하지 않아도 됨"으로 보류)
- 등록되지 않은 새로운 자막 스타일은 자동 인식이 안 됨 → 사용자가 그때그때 새 참고 이미지로 등록해야 함 (미분류 누적 시 등록 부담 증가 가능)
- 자막 ROI가 영상/프로그램마다 다를 수 있음 → 초기엔 수동 지정, 추후 자동 검출 검토
- "움직임이 멈춘 정지 프레임" 판정 임계값이 너무 낮으면 애니메이션 도중을 정지로 오판할 수 있음 → 튜닝 필요
- 지원할 효과 타입(참고 이미지) 목록/우선순위 미확정 (PRD Open Question과 동일 이슈)
- 성능: 긴 영상(예능 풀편, 1시간 내외) 처리 시간 — 초기엔 실시간성 없이 배치 처리로 시작해도 무방
- 웹 서비스 전환에 따른 신규 리스크: 업로드 영상/효과음 파일이 저작권 있는 콘텐츠일 수 있어, 저장소(Supabase Storage)에 보관 시 사용자별 접근 제어를 확실히 해야 함 (남의 것이 노출되지 않도록)

## 8. 다음 대화에서 할 일
1. Supabase 프로젝트 생성 (M4)
2. FastAPI에 Supabase Auth JWT 검증 붙이기

## 9. 향후 확장
- REAPER ReaScript 연동 (M9)
- 믹스된 완성본 오디오/영상 자동 생성 옵션 (M10, 선택적 — 기본 출력은 계속 CSV/EDL 표)
- 지원 효과 타입 확대, 자동 ROI(자막 영역) 검출, ML 기반 분류기 도입
- 소셜 로그인, 구독/결제 시스템 (이번 MVP 범위 밖)
- 트래픽 증가 시 Redis 기반 큐로 백그라운드 처리 교체

## 10. 출시 전 보안·운영 체크리스트 (실제 사용자를 받기 전 필수)

> 개발 중(M4~M7)에는 아래 항목을 몰라도 진행 가능하지만, **실제로 남들이 가입해서 쓰게 하기 전에는 반드시 확인**해야 한다. M4에서 Supabase를 처음 설정할 때 같이 켜두면 나중에 다시 손댈 일이 줄어든다.

| # | 항목 | 이 프로젝트에서 할 일 | 시점 |
|---|---|---|---|
| 1 | 비밀 키 관리 | Supabase의 `anon`(공개) 키는 프론트엔드에 둬도 되지만, **`service_role`(비밀) 키는 절대 프론트엔드/공개 저장소에 넣지 않고 FastAPI 서버 환경변수(`.env`, gitignore 처리)에만** 보관. 실수로 한 번이라도 노출됐다면 Supabase 대시보드에서 즉시 재발급 | M4 (지금 바로 `.gitignore`에 `.env` 추가) |
| 2 | 이메일 확인 + 커스텀 SMTP | Supabase Auth 설정에서 "Confirm email" 켜기. 기본 내장 메일은 발송량 제한이 크므로 Resend/SendGrid/SES/Postmark 중 하나를 SMTP로 연결 | M4 |
| 3 | 비밀번호 정책 + 유출 검사 | Supabase Auth 설정에서 최소 길이/복잡도 규칙 설정 + "Leaked password protection"(HaveIBeenPwned 연동) 켜기 (요금제별 지원 여부 확인 필요) | M4 |
| 4 | 남용 방어 | 가입/로그인 폼에 CAPTCHA(hCaptcha 또는 Cloudflare Turnstile — Supabase Auth가 연동 지원) 적용, FastAPI 쪽에도 요청 제한(rate limit) 미들웨어 추가 | M4~M5 |
| 5 | 접근 제어(RLS) 재점검 | `Project`, `Event`, `ReferenceImage` 등 모든 테이블에 **Row Level Security 활성화** + "본인 `user_id` 행만 조회/수정 가능" 정책 작성. Supabase의 Security Advisor를 주기적으로 실행해서 누락된 정책 확인 | M4~M5, 이후 주기적 점검 |
| 6 | 도메인·주소 설정 | 실제 배포 도메인(Vercel 커스텀 도메인 등)으로 Supabase Auth의 Site URL / Redirect URL 허용 목록을 정확히 맞추기, HTTPS 강제 | 배포 직전 |
| 7 | 운영 기본기 | Supabase 자동 백업 설정 확인, 로그/에러 모니터링(Supabase 대시보드 + Render/Fly 로그) 확인, 이메일 등 개인정보 보관·삭제(회원 탈퇴 시 처리) 정책 정하기 | 출시 직전 |
| 8 | 무료 티어 한계 인지 | Supabase 무료 프로젝트는 일정 기간 미사용 시 일시 정지(콜드 스타트)될 수 있고, 이메일 발송/DB 용량에도 한도가 있음. Render/Vercel 무료 티어도 유휴 시 콜드 스타트 있음 → 실사용자가 늘면 유료 플랜 전환 계획 필요 | 실사용 시작 시점 |

이 체크리스트는 **M4(Supabase 설정) 중 1~5번을 함께 처리**하고, **실제 오픈 직전에 6~8번을 최종 점검**하는 흐름으로 진행한다.
